name: Publicar Instagram, Facebook e TikTok

on:
  schedule:
    # Todos os dias às 09:00 de Brasília (12:00 UTC).
    - cron: "0 12 * * *"
  workflow_dispatch:
    inputs:
      data_publicacao:
        description: "Data do item da fila (AAAA-MM-DD); vazio usa hoje"
        required: false
        type: string
      plataformas:
        description: "Plataformas desta execução: instagram,facebook,tiktok"
        required: false
        default: "instagram,facebook,tiktok"
        type: string

concurrency:
  group: publicacao-diaria
  cancel-in-progress: false

jobs:
  publicar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Instalar dependências
        run: pip install -r requirements.txt
      - name: Publicar conteúdo do dia
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_BUSINESS_ID: ${{ secrets.IG_BUSINESS_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          TIKTOK_CLIENT_KEY: ${{ secrets.TIKTOK_CLIENT_KEY }}
          TIKTOK_CLIENT_SECRET: ${{ secrets.TIKTOK_CLIENT_SECRET }}
          TIKTOK_REFRESH_TOKEN: ${{ secrets.TIKTOK_REFRESH_TOKEN }}
          TIKTOK_REFRESH_TOKEN_FILE: ${{ runner.temp }}/tiktok_refresh_token
          # O YouTube é programado diretamente no YouTube Studio pelo navegador.
          # Manter false evita publicação duplicada pelo robô.
          ATIVAR_YOUTUBE: "false"
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          DATA_PUBLICACAO: ${{ inputs.data_publicacao }}
          PLATAFORMAS: ${{ inputs.plataformas || 'instagram,facebook,tiktok' }}
        run: python publicar.py
      - name: Guardar refresh token renovado do TikTok
        if: always()
        env:
          GH_TOKEN: ${{ secrets.TIKTOK_SECRETS_PAT }}
        run: |
          if [ -s "$RUNNER_TEMP/tiktok_refresh_token" ] && [ -n "$GH_TOKEN" ]; then
            gh secret set TIKTOK_REFRESH_TOKEN < "$RUNNER_TEMP/tiktok_refresh_token"
          fi
      - name: Salvar resultado e remover mídias concluídas
        if: always()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add fila/fila.json
          git add -u videos/
          git diff --staged --quiet || git commit -m "chore: atualizar publicação $(date +%F)"
          git push

